// 🚀 Transforme a função hello em uma arrow function.
// Copie o código a seguir.

// function hello(nome) {
//   return `Olá, ${nome}!`;
// }
// let nome = "Ivan";
// console.log(hello(nome));

const hello = (nome) => {
  return `Olá, ${nome}!`;
};
let nome = "Ivan";
console.log(hello(nome));

// 2 – Crie uma função que calcule a área de um círculo


const calcularAreaCirculo = (raio) => {
const area = Math.PI * (raio ** 2); 
console.log(A área do círculo de raio ${raio} é ${area.toFixed(2)}.); 
return area;
};

// Math.PI é uma propriedade da biblioteca do JavaScript que retorna o valor da  constante matematica PI que representa a relação entre a circunferência de um circulo e seu diâmetro. O valor retornado pela propriedade Math.PI é uma aproximação do valor de PI com 15 dígitos decimais.

// O método toFixed() recebe como parâmetro o número de casas decimais que se deseja manter